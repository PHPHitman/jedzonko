{% extends 'base.html.twig' %}


{% block body %}
<div class="container">
    {{ block ('flashmessage', 'base.html.twig') }}
        {%  block menu %}
            <div class="foodcard">

                    <div class="description">
                        <h2>Dzisiejsze zamówienie</h2><hr/>
                        {%  for order in orders %}
                         {{ order.food }}
                            <button type="button" class="btn btn-success"><a href="{{ path('details', {id: order.id}) }}">Szczegóły</a></button>
                            <button type="button" class="btn btn-success">Edytuj</button>
                        {% endfor %}
                    </div>

            </div>


            <div class="row">
                <div class="col-12 col-xl-6">
                    <div class="card"><a href="{{ path('order') }}">Złóż zamówienie</a></div>
                </div>
                <div class="col-12 col-xl-6">
                    <div class="card"><a href="#">Historia</a></div>
                </div>
                <div class="col-12 col-xl-6">
                    <div class="card"><a href="#">Statystyki</a></div>
                </div>
            </div>
                {% endblock %}
            </div>

{% endblock %}